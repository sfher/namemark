name: build

on:
  push:
    branches: [main, linux-compat]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  msvc:
    name: Windows (MSVC)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Compile
        run: .\build.bat
      - name: Verify binary
        run: |
          if (Test-Path namemark.exe) {
            $size = (Get-Item namemark.exe).Length
            Write-Host "Build SUCCESS: namemark.exe ($size bytes)"
          } else {
            Write-Error "namemark.exe not found"
          }
      - uses: actions/upload-artifact@v4
        with:
          name: namemark-windows-msvc
          path: namemark.exe

  gcc:
    name: Linux (GCC)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Compile
        run: make -j$(nproc)
      - name: Verify binary
        run: test -x namemark && echo "Build SUCCESS"
      - uses: actions/upload-artifact@v4
        with:
          name: namemark-linux-gcc
          path: namemark
